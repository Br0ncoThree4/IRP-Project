#include <iostream>
#include <string>
#include <list>
#include "Chess.cpp"
using namespace std;

class Board {
    public:
        Chess
        Board(string, int, int);
    private:

    


    Board::Board(string, int r, int f) {//Creates the 2D array that is the board
        Chess theBoard[8][8];

    }
}
